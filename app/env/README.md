This is directory for environments.

Documentation: https://github.com/vlucas/phpdotenv

**NOTE** Each file in that directory is value of `APP_ENV` variable, set in Dockerfile (or docker-compose.yml)
